{
  "name": "@gaiatec/documentacao",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "format": "prettier --write . --ignore-unknown",
    "format:check": "prettier --check . --ignore-unknown",
    "check:docs": "node scripts/check-docs.mjs",
    "check": "npm run format:check && npm run check:docs"
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
